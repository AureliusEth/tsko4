# Required: a random secret used to sign voter ID cookies
COOKIE_SECRET=change-me-to-a-random-secret

# PostgreSQL password (used by both the db container and the app)
POSTGRES_PASSWORD=change-me-to-a-random-password
